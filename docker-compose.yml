version: '3.8'

services:
  f1-epg-server:
    image: ghcr.io/chris-grant1995/f1-epg-server:latest
    ports:
      - "5001:5001"
    environment:
      # Default timezone if not specified via command-line args
      # You can override this in the command below
      - TZ=America/New_York
    
    restart: unless-stopped
